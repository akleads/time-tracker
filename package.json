{
  "name": "time-tracker",
  "version": "1.0.0",
  "description": "Time-based URL redirector with multi-user support and statistics",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "build": "echo 'No build step required' && exit 0",
    "init-db": "node scripts/init-db.js"
  },
  "keywords": [
    "redirect",
    "time-based",
    "url-shortener",
    "campaign",
    "analytics"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@libsql/client": "^0.14.0",
    "express": "^4.18.2",
    "express-session": "^1.17.3",
    "bcrypt": "^5.1.1",
    "date-fns": "^3.3.1"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
